# packages/runtime — Runtime Estimation Engine

Estimates and reasons about runtime behavior of analyzed code.

Responsible for: runtime estimation, execution measurement, input scaling, CPU trend analysis, execution statistics.

**Rule:** Must always distinguish estimated runtime from measured runtime; never present one as the other.
