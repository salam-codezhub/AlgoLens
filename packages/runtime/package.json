{
  "name": "@algolens/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime estimation engine (estimated vs. measured runtime).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
