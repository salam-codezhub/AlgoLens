{
  "name": "@algolens/optimizer",
  "version": "0.1.0",
  "private": true,
  "description": "Optimization suggestions and patch/diff generation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
