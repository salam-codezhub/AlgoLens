{
  "name": "@algolens/security",
  "version": "0.1.0",
  "private": true,
  "description": "Security scanning: threat, secret, and injection detection.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
