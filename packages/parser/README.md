# packages/parser — Parsing & AST Engine

Turns source files into ASTs and tokens across supported languages.

Responsible for: Tree-sitter integration, Babel, Java parser, Python AST, parsing, tokenization, AST creation, language detection.
