# packages/core — Core Business Logic

Core business logic, interfaces, shared domain models, the Event Bus, configuration, and dependency injection wiring that the rest of the system is built on.

Responsible for: core business logic, interfaces, shared models, domain objects, Event Bus, configuration, dependency injection.

**Rule:** Never imports UI code.
