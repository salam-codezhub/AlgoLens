# packages/ui — Shared UI Component Library

Reusable, presentation-only React components shared across the webview app.

Responsible for: reusable components, cards, dialogs, buttons, navigation, theme, charts, icons.
