# packages/benchmark — Benchmark Engine

Executes and measures real runtime behavior of analyzed code to validate estimated complexity against actual performance.

Responsible for: benchmark execution, input-size scaling runs, timing collection, statistical aggregation of measured results (feeds `packages/runtime`'s measured-vs-estimated comparison).
