# tests/ — Cross-Package Test Suites

Root for integration and end-to-end tests that span multiple packages or apps (unit tests live alongside the code they test, inside each package). Structure (unit/integration/e2e runners, fixtures, test utilities) will be introduced in Phase 38 — Testing Framework.

Currently empty by design: no test tooling is configured yet (that begins in Phase 03 — Package Manager Setup and is completed in Phase 38).
